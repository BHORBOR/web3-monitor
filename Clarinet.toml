[project]
name = "web3-monitor"
authors = ["Web3 Protocol Team"]
description = "Web3 activity tracking and monitoring smart contract"
telemetry = false
requirements = []
[contracts.web3-activity-tracker]
path = "contracts/web3-activity-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
